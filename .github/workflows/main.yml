name: Build and Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Backend
        run: docker build -t technologii_chmurowe-backend ./backend
      - name: Build Frontend
        run: docker build -t technologii_chmurowe-frontend ./frontend
